import { ActiveLabelDirective } from './active-label.directive';

describe('ActiveLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveLabelDirective();
    expect(directive).toBeTruthy();
  });
});
